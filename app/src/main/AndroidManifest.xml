<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chucknorrisjokes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:name=".di.Application"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChuckNorrisJokes">
        <activity android:name=".presentation.ui.JokeActivity" />
        <activity android:name=".presentation.ui.MainActivity" />
        <activity
            android:name=".presentation.ui.SplashActivity"
            android:noHistory="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>